import { Sora, Work_Sans } from 'next/font/google';

export const sora = Sora({
  variable: '--font-title',
  subsets: ['latin'],
});

export const workSans = Work_Sans({
  variable: '--font-body',
  subsets: ['latin'],
});
